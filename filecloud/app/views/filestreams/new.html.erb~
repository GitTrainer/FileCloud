<h1>UpLoadFile to<%=@folder.name%></h1>

<%= form_for(@fileupload,:html => { :multipart => true }) do |f| %>
    <p>
      <%= f.hidden_field :folder_id,:value=> params[:folder_id] %>
      <%= f.label :FileUpLoad %>
      <%= f.file_field :attach, :accept=> "image/*" %>
     </p>
     <p>
      <%= f.submit "Upload" %>
    <% end %>
    </p>
    <p>
    <%=link_to 'Back',{:action=>'show',:controller=>'folders',:id=>@folder.id}%>
    </p>
