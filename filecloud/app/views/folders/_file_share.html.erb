
<h5>Choose member to share file</h5>
<% @list_users = User.find_by_sql(["select * from users where users.id != ?",current_user])%>
<% @list_users.each do |user|%>
<% select=false
Filesharing.where(:shared_user_id => user.id, :file_id => params[:file_id]).each  do |pm|
select=true
end %>
<%= check_box_tag 'activated[]', user.id,select -%>
<%= user.name%>
<br/>
<%end%>
